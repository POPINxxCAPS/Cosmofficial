<!DOCTYPE html>
<html lang="en">

<%- include("./partials/_header") %>

<body>
    <div class="container">
        <div class="row">
            <div class="col offset-md-1">
                <div class="btn-toolbar">
                    <div class="btn-group">
                        <a href="https://cosmofficial.herokuapp.com/" class="btn btn-success">Home</a>
                        <a href="https://discord.gg/BfFc8cfp3n" class="btn btn-info">Discord</a>
                        <a href="https://discord.com/api/oauth2/authorize?client_id=845784381025157131&permissions=8&scope=bot"
                            class="btn btn-primary">Invite Cosmofficial</a>
                        <a href="https://www.patreon.com/Cosmofficial" class="btn btn-secondary">Patreon</a>
                        <% if(authURL === "Logged-In") { %>
                        <a class="btn btn-danger">Sign-Out</a>
                        <% } else { %>
                            <a href="<%= authURL %>" class="btn btn-success">Log-In</a>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="container">
        <% servers.forEach(server => { %>
        <div class="card mt-4">
            <div class="card-body">
                <h4 class="card-title"><%= server.serverName %></h4>
                <div class="card-subtitle text-muted mb-2">
                    <%= `${server.players} Players Online` %>
                </div>
                <div class="row">
                    <div class="btn-toolbar">
                        <div class="btn-group">
                            <a href="<%= server.inviteLink %>" class="btn btn-success">Discord</a>
                            <a href="servers/<%= server.guildID %>" class="btn btn-primary">Select Server</a>
                            <a href="grids/<%= server.guildID %>" class="btn btn-primary">Grids</a>
                            <a href="users/<%= server.guildID %>" class="btn btn-primary">Players</a>
                            <a href="factions/<%= server.guildID %>" class="btn btn-primary">Factions</a>
                            <!-- <a href="factions/<%= server.serverName %>" class="btn btn-danger">Admin Area</a> -->
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <% }) %>
    </div>
</body>

<%- include("./partials/_footer") %>

</html>